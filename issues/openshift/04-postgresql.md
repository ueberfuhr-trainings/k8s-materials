---
layout: default
title: "Übung 4: PostgreSQL-Datenbank"
---

# PostgreSQL-Datenbank in OpenShift bereitstellen

Als Entwickler möchte ich eine PostgreSQL-Datenbank in OpenShift deployen und die Zugangsdaten sicher in Secrets verwalten, damit das Backend später auf eine echte Datenbank umgestellt werden kann.

## 🎯 Lernziele

* Du kannst eine Datenbank als Container in Kubernetes/OpenShift deployen.
* Du verstehst den Unterschied zwischen ConfigMaps und Secrets.
* Du kannst Secrets erstellen und in Deployments verwenden.
* Du kannst eine ConfigMap nutzen, um Initialisierungs-Skripte in einen Container einzubinden (Volume Mount).

## ✅ Definition of Done

* [ ] Du hast ein PostgreSQL-Deployment und einen Service erstellt.
* [ ] Du hast die Zugangsdaten (Username, Passwort) in einem Secret abgelegt.
* [ ] Du hast den Datenbanknamen in einer ConfigMap abgelegt.
* [ ] Du hast das SQL-Schema über eine ConfigMap als Init-Skript eingebunden.
* [ ] Die Datenbank läuft und die Tabellen sind angelegt.

## 🪜 Arbeitsschritte

### 1. Secret für Zugangsdaten erstellen

Erstelle eine Datei `postgres-secret.yaml`:

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  POSTGRES_USER: recipes
  POSTGRES_PASSWORD: recipes-secret-pw
```

```bash
oc apply -f postgres-secret.yaml
```

### 2. ConfigMap für Datenbanknamen erstellen

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
data:
  POSTGRES_DB: recipes
```

```bash
oc apply -f postgres-config.yaml
```

### 3. ConfigMap für das Init-SQL erstellen

Das Backend erwartet ein bestimmtes Datenbankschema. Erstelle eine ConfigMap mit dem Initialisierungs-SQL:

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-sql
data:
  init.sql: |
    CREATE TABLE recipes (
        id          VARCHAR(36)   PRIMARY KEY,
        name        VARCHAR(100)  NOT NULL,
        img         VARCHAR(255),
        servings    INTEGER       NOT NULL,
        lastEdited  TIMESTAMP WITH TIME ZONE,
        duration    INTEGER       NOT NULL,
        difficulty  VARCHAR(10),
        preparation VARCHAR(2000) NOT NULL
    );

    CREATE TABLE ingredients (
        id        BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        name      VARCHAR(100) NOT NULL,
        quantity  DOUBLE PRECISION NOT NULL,
        unit      VARCHAR(20)  NOT NULL,
        recipe_id VARCHAR(36)  REFERENCES recipes(id) ON DELETE CASCADE
    );
```

```bash
oc apply -f postgres-init-sql.yaml
```

### 4. PostgreSQL-Deployment erstellen

> **Hinweis:** Wir verwenden hier der Einfachheit halber ein Deployment ohne Persistent Volume. Für produktive Szenarien wäre ein **StatefulSet** mit PersistentVolumeClaim die richtige Wahl — siehe [PostgreSQL auf Kubernetes – Best Practices](../../docs/postgres-as-statefulset.html).

Erstelle eine Datei `postgres-deployment.yaml`. Das offizielle PostgreSQL-Image führt automatisch alle `.sql`-Dateien im Verzeichnis `/docker-entrypoint-initdb.d/` beim ersten Start aus.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          envFrom:
            - secretRef:
                name: postgres-secret
            - configMapRef:
                name: postgres-config
          volumeMounts:
            - name: init-sql
              mountPath: /docker-entrypoint-initdb.d
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
      volumes:
        - name: init-sql
          configMap:
            name: postgres-init-sql
        - name: data
          emptyDir: {}
```

```bash
oc apply -f postgres-deployment.yaml
```

### 5. Service erstellen

```yaml
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
```

```bash
oc apply -f postgres-service.yaml
```

### 6. Prüfung

Prüfe, ob der PostgreSQL-Pod läuft und die Tabellen angelegt wurden:

```bash
oc get pods
oc logs <postgres-pod-name>
```

In den Logs sollte die Ausführung des Init-Skripts sichtbar sein.

## 📚 Selbstlernmaterial

* [Kubernetes Secrets](https://kubernetes.io/docs/concepts/configuration/secret/)
* [Kubernetes Volumes](https://kubernetes.io/docs/concepts/storage/volumes/)
* [ConfigMap als Volume einbinden](https://kubernetes.io/docs/tasks/configure-pod-container/configure-pod-configmap/#add-configmap-data-to-a-volume)
* [PostgreSQL Docker Image](https://hub.docker.com/_/postgres) — Dokumentation der Umgebungsvariablen und Init-Skripte
* [PostgreSQL auf Kubernetes – Best Practices](../../docs/postgres-as-statefulset.html) — StatefulSet, Operatoren und produktionsreife Setups

## 🤔 Reflexionsfragen

* Warum werden Zugangsdaten in einem Secret statt in einer ConfigMap gespeichert? Wie sicher ist ein Kubernetes-Secret wirklich?
* Warum wird das Init-SQL über eine ConfigMap und ein Volume eingebunden, statt es direkt ins Image zu packen?
* Was ist der Unterschied zwischen `envFrom: secretRef` und einzelnen `env`-Einträgen mit `valueFrom: secretKeyRef`?
* Inwiefern ist unser PostgreSQL-Setup in dieser Übung nicht produktionsreif?
